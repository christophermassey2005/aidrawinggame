using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PointsOOPManager : MonoBehaviour
{
    private PointsOOP runpoints;

    // Start is called before the first frame update
    void Start()
    {
        PointsOOP runpoints = new PointsOOP();
    }

}
